[project]
name = "k8s-stack-client-example-com"
version = "0.0.0"
description = "Validation dependencies for the Example client configuration"
requires-python = ">=3.12"
dependencies = [
    "pyyaml>=6.0,<7",
]

[tool.uv]
package = false
